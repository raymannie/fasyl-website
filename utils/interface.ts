export interface Partner {
  name: string;
  logo: string;
}

export interface Partner {
  name: string;
  logo: string;
}

export interface ProfileProps {
  name: string;
  description: string;
  image: string;
  about: string;
  category: string;
}
